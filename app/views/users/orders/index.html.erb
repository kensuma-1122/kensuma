<% provide(:title,  "発注一覧(元請)") %>

<%= link_to "発注情報作成", new_users_order_path, class: "btn btn-lg btn-block btn-outline-primary" %>
<br>
<% if @orders.present? %>
  <div class="card">
    <div class="card-body table-responsive p-0">
      <table class="table table-hover text-nowrap">
        <thead>
          <tr align="center">
            <th><%= Order.human_attribute_name(:site_name) %></th>
            <th><%= Order.human_attribute_name(:status) %></th>
          </tr>
        </thead>
        <tbody>
          <% @orders.each do |order| %>
          <tr align="center">
            <td><%= link_to order.site_name, users_order_path(order) %></td>
            <td><%= order.status_i18n %></td>
          </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
<% end %>
