<% provide(:title, "発注依頼編集(下請)") %>
<% provide(:btn_text, "更新") %>

<div class="card">
  <%= form_with model: @request_order, url: users_request_order_path, method: :patch, local: true, class: "order-form-validation" do |f| %>
    <div class="card-body">
      <%= render partial: "form", locals: { f: f, request_order: @request_order} %>
    </div>
    <div class="card-footer">
      <%= f.submit yield(:btn_text), class: "btn btn-md btn-block btn-success" %>
      <%= link_to "詳細画面へ", users_request_order_path(@request_order), class: "btn btn-md btn-block btn-default" %>
      <%= link_to "一覧画面へ", users_request_orders_path, class: "btn btn-md btn-block btn-default" %>
    </div>
  <% end %>
</div>
